# Designing with AI Hero — Pass 2 Correction

This artifact preserves Pass 2 content, presentation primitives, and choreography while correcting the hero's final viewport footprint using `reference_hero-final-size-estimate.png`.

Desktop now reads as a compact editorial module rather than a landing-page hero. Stage height, panel geometry, extrusion, type, copy measure, CTA, spacing, technical density, and the empty Pass 3 visual region respond independently. Tablet and mobile grow vertically as content reflows.

Ambient background events, sparkles, CTA motion polish, and the supporting AI workflow visualization are intentionally deferred to Pass 3.

Open `index.html` for the isolated preview and integration notes. Use `component.html` with the shared global token, local token, component, and module stylesheets.

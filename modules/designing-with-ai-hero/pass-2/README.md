# Designing with AI Hero — Pass 2

Pass 2 preserves the approved technical background, dimensional panel, responsive geometry, container entrance, and reduced-motion baseline from Pass 1.

It adds the shared portfolio heading pattern, shared primary button, final eyebrow/title/body/CTA content, semantic accent color, and a restrained content sequence. The title materializes from a short 45-degree offset; the eyebrow uses a smaller clipped reveal; copy and CTA follow with quieter motion.

Ambient background events, sparkles, CTA motion polish, and the supporting AI workflow visualization are intentionally deferred to Pass 3.

Open `index.html` for the isolated preview and integration notes. Use `component.html` with the shared global token, local token, component, and module stylesheets.

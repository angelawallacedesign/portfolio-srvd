# Standalone Components

These components are extracted from the case-study page and have no dependency on the original portfolio CSS, tokens, layout utilities, or JavaScript modules.

## Privacy Hero

Path: `privacy-hero/`

- Copy `paste-ready.html` for a single dependency-free HTML block.
- Or copy `component.html`, `component.css`, and `component.js` if you prefer split files.
- Open `index.html` to preview the split-file version.

## State Table

Path: `state-table/`

- Copy `paste-ready.html` for a single dependency-free HTML block.
- Or copy `component.html`, `component.css`, and `component.js` if you prefer split files.
- Open `index.html` to preview the split-file version.
